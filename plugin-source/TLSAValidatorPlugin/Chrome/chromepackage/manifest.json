{
  "name": "TLSAValidatorPlugin",
  "version": "${FBSTRING_PLUGIN_VERSION}",
  "description": "Plug-in used by TLSA Validator extension",

	"plugins": [
    { "path": "npTLSAValidatorPlugin.dll", "public": true }
  ]
}